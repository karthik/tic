on:
  push:

name: docker

jobs:
  all:
    runs-on: ubuntu-latest

    name: docker

    steps:
        - uses: docker/build-push-action@v1
          with:
            username: ${{ secrets.DOCKER_USERNAME }}
            password: ${{ secrets.DOCKER_PASSWORD }}
            repository: ropensci/tic
            tags: latest
